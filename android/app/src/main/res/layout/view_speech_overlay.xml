<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/speechOverlay"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/colorBackground"
    android:clickable="true"
    android:focusable="true"
    android:visibility="gone">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="24dp"
        android:paddingTop="48dp"
        android:paddingBottom="24dp">

        <!-- Recording indicator area -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp">

            <!-- Pulsing ring shown while recording -->
            <View
                android:id="@+id/speechPulseRing"
                android:layout_width="96dp"
                android:layout_height="96dp"
                android:layout_gravity="center"
                android:background="@drawable/bg_record_button"
                android:alpha="0.3"
                android:visibility="gone" />

            <!-- Mic icon -->
            <ImageView
                android:id="@+id/speechMicIcon"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_mic_circle"
                android:background="@drawable/bg_record_button"
                android:padding="20dp"
                android:importantForAccessibility="no" />
        </FrameLayout>

        <!-- Status label -->
        <TextView
            android:id="@+id/speechStatusLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/speech_status_listening"
            android:textSize="16sp"
            android:layout_marginBottom="24dp" />

        <!-- Transcript input -->
        <EditText
            android:id="@+id/speechEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/hint_food_name"
            android:inputType="text"
            android:minHeight="48dp"
            android:paddingHorizontal="16dp"
            android:paddingVertical="12dp"
            android:layout_marginBottom="24dp" />

        <!-- Start / Stop toggle button -->
        <ImageButton
            android:id="@+id/speechToggleBtn"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_record_button"
            android:src="@drawable/ic_stop_square"
            android:padding="16dp"
            android:contentDescription="@string/cd_speech_toggle" />

        <!-- Spacer pushes bottom bar down -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Bottom action buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/speechCancelBtn"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="4dp"
                android:text="@string/speech_btn_cancel" />

            <Button
                android:id="@+id/speechApplyBtn"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="4dp"
                android:text="@string/btn_apply" />
        </LinearLayout>

    </LinearLayout>
</FrameLayout>
